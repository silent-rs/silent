# TODO（性能优化第一阶段：零成本抽象改造）

> 分支: `perf/framework-optimization`
> 目标版本: v2.12
> 优先级: P0 - 立即开始
> 状态: 🔴 进行中
> 时间范围: 2025-12-01 ~ 2025-12-31
> 预期收益: 延迟降低 15-25%，吞吐提升 15-20%

## 目标

基于性能瓶颈分析，通过零成本抽象改造快速获得性能提升：
- **消除 async_trait 堆分配**：91 处 Box::pin 减少到 ≤10 处
- **优化 Future 返回值**：消除热路径上的动态分发
- **减少对象克隆**：审计并移除不必要的 `.clone()` 调用
- **建立性能基线**：集成 criterion benchmark 和性能监控

## 性能目标
- [ ] criterion benchmark 延迟降低 ≥15%
- [ ] wrk 测试吞吐提升 ≥15%
- [ ] 火焰图显示堆分配减少 ≥50%
- [ ] 所有单元测试和集成测试通过
- [ ] 编译无 warning

---

## P0-1: 移除 async_trait（第 1-2 周）

**优先级**: P0 - 最高
**时间**: 2025-12-01 ~ 2025-12-14 (2 周)
**负责人**: 待定
**预期收益**: 10-15% 延迟降低

### 目标
将所有 async trait 从 `async_trait` 宏迁移到 RPITIT (Return Position Impl Trait In Traits) 或手动实现。

### 子任务

#### 1.1 Handler trait 重构
- [ ] **研究 RPITIT 方案**（1 天）
  - [ ] 验证 Rust 版本兼容性（需要 1.75+）
  - [ ] 确定使用 RPITIT 还是手动关联类型
  - [ ] 编写 POC 验证编译器支持
  - 位置: `silent/src/handler/handler_trait.rs:10-12`

- [ ] **重构 Handler trait 定义**（2 天）
  - [ ] 移除 `#[async_trait]` 宏
  - [ ] 修改 `call` 方法签名为 RPITIT 或关联类型
  - [ ] 更新所有 Handler 实现（HandlerFn, HandlerWrapper 等）
  - 位置: `silent/src/handler/`

- [ ] **更新 HashMap<Method, Arc<dyn Handler>> 实现**（1 天）
  - [ ] 调整方法分发逻辑
  - [ ] 确保 HEAD 回退到 GET 的逻辑正确
  - 位置: `silent/src/handler/handler_trait.rs:15-42`

#### 1.2 MiddleWareHandler trait 重构
- [ ] **重构 MiddleWareHandler trait**（2 天）
  - [ ] 移除 `#[async_trait]`
  - [ ] 修改 `handle` 方法签名
  - [ ] 更新所有中间件实现
  - 位置: `silent/src/middleware/mod.rs`

- [ ] **更新内置中间件**（2 天）
  - [ ] Cors 中间件
  - [ ] Timeout 中间件
  - [ ] ExceptionHandler 中间件
  - [ ] RequestTimeLogger 中间件
  - 位置: `silent/src/middlewares/`

#### 1.3 RouteTree 和路由系统调整
- [ ] **调整 RouteTree Handler 存储**（1 天）
  - [ ] 更新 `handler: HashMap<Method, Arc<dyn Handler>>` 的泛型约束
  - [ ] 调整 `call` 方法实现
  - 位置: `silent/src/route/route_tree.rs:101-111`

#### 1.4 测试和验证
- [ ] **单元测试修复**（1 天）
  - [ ] 修复所有受影响的单元测试
  - [ ] 确保测试覆盖率不降低

- [ ] **集成测试验证**（1 天）
  - [ ] 运行所有示例验证功能正确
  - [ ] examples/hello-world
  - [ ] examples/websocket-chat
  - [ ] examples/grpc

- [ ] **性能基准测试**（1 天）
  - [ ] 运行 criterion benchmark
  - [ ] 对比优化前后性能数据
  - [ ] 生成火焰图分析堆分配
  - 位置: `benchmark/benches/route_benchmark.rs`

### 验收标准
- [ ] 所有 async_trait 宏使用从 Handler/MiddleWare 相关代码中移除
- [ ] `cargo check --all-features` 通过
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` 通过
- [ ] `cargo test --all-features` 全量通过
- [ ] criterion benchmark 显示延迟降低 ≥10%
- [ ] 火焰图显示 Handler 调用路径堆分配显著减少

### 风险与缓解
- **风险**: API 兼容性破坏，影响下游用户
  - **缓解**: 保留旧 API 作为 deprecated 过渡，提供 v2.12 到 v2.13 迁移指南
- **风险**: 大量代码改动可能引入 bug
  - **缓解**: 分模块逐步迁移，每个模块完成后立即测试

---

## P0-2: 消除 Box::pin 堆分配（第 2 周）

**优先级**: P0
**时间**: 2025-12-08 ~ 2025-12-14 (与 P0-1 并行)
**负责人**: 待定
**预期收益**: 3-5% 延迟降低

### 目标
消除 HyperServiceHandler 和其他热路径上的 Box::pin 堆分配。

### 子任务

#### 2.1 HyperServiceHandler 优化
- [ ] **重构 Future 返回类型**（1 天）
  - [ ] 将 `type Future = Pin<Box<dyn Future...>>` 改为具体类型
  - [ ] 使用 `impl Future` 或关联类型
  - 位置: `silent/src/server/protocol/hyper_http/hyper_service.rs:58`

- [ ] **优化 call 方法实现**（1 天）
  - [ ] 移除 `Box::pin(async move { ... })`
  - [ ] 直接返回 Future
  - 位置: `silent/src/server/protocol/hyper_http/hyper_service.rs:61-102`

#### 2.2 其他 Box::pin 审计
- [ ] **全局搜索 Box::pin 使用**（0.5 天）
  - [ ] 使用 grep 找出所有 91 处 Box::pin
  - [ ] 分类：热路径 vs 冷路径
  - [ ] 优先优化热路径

- [ ] **优化其他热路径 Box::pin**（1 天）
  - [ ] Listen trait 的 AcceptFuture（如果可行）
  - [ ] ConnectionService trait
  - 位置: `silent/src/server/listener.rs`, `silent/src/server/connection_service.rs`

### 验收标准
- [ ] HyperServiceHandler 不再使用 Box::pin
- [ ] 热路径上的 Box::pin 减少 ≥80%
- [ ] criterion benchmark 显示额外 3-5% 延迟降低
- [ ] perf/flamegraph 显示堆分配进一步减少

---

## P0-3: 减少克隆操作审计（第 2-3 周）

**优先级**: P0
**时间**: 2025-12-08 ~ 2025-12-14 (1 周)
**负责人**: 待定
**预期收益**: 5-8% CPU 使用率降低

### 目标
审计并优化热路径上的克隆操作，减少不必要的内存分配。

### 子任务

#### 3.1 克隆审计
- [ ] **识别热路径克隆点**（1 天）
  - [ ] 使用 cargo-flamegraph 识别克隆热点
  - [ ] 审计所有 `.clone()` 调用
  - [ ] 重点关注: Request, Response, Handler, SocketAddr

- [ ] **分类克隆操作**（0.5 天）
  - [ ] 必要克隆（需要所有权转移）
  - [ ] 可优化克隆（可用引用/Cow 替代）
  - [ ] 不必要克隆（完全可消除）

#### 3.2 优化实施
- [ ] **HyperServiceHandler 克隆优化**（1 天）
  - [ ] 优化 `routes.clone()`（hyper_service.rs:46）
  - [ ] 优化 `remote_addr.clone()`（hyper_service.rs:45）
  - 方案: 考虑使用 Arc 或引用
  - 位置: `silent/src/server/protocol/hyper_http/hyper_service.rs:44-49`

- [ ] **NetServer 连接处理克隆优化**（1 天）
  - [ ] 优化 `handler.clone()`（net_server.rs:497, 556）
  - [ ] 优化 `peer_addr.clone()`（net_server.rs:498, 557）
  - 位置: `silent/src/server/net_server.rs:485-595`

- [ ] **Response copy_from_response 优化**（1 天）
  - [ ] 分析 copy_from_response 的必要性
  - [ ] 考虑使用移动语义替代复制
  - 位置: `silent/src/handler/handler_trait.rs:22, 32`

- [ ] **引入 Cow<'_, str> 优化**（1 天）
  - [ ] 路径参数提取使用 Cow
  - [ ] 避免不必要的 String 分配
  - 位置: `silent/src/route/route_tree.rs`

#### 3.3 验证
- [ ] **性能验证**（0.5 天）
  - [ ] criterion benchmark 对比
  - [ ] CPU profiling 验证使用率降低
  - [ ] 内存分配统计

### 验收标准
- [ ] 热路径克隆次数减少 ≥30%
- [ ] CPU 使用率降低 5-8%
- [ ] criterion benchmark 显示性能进一步提升
- [ ] 无内存泄漏或所有权问题

---

## P1: 泛型化 Handler 系统（第 3-4 周，可选）

**优先级**: P1 - 高优先级（视 P0 完成情况）
**时间**: 2025-12-15 ~ 2025-12-21 (2 周)
**预期收益**: 5-10% 吞吐提升

### 目标
将 `Arc<dyn Handler>` 替换为泛型 Handler，实现单态化优化。

### 子任务
- [ ] **设计泛型化方案**（2 天）
  - [ ] RouteTree<H> 泛型设计
  - [ ] 评估编译时间和二进制大小影响
  - [ ] 制定迁移策略

- [ ] **实现 RouteTree 泛型化**（3 天）
  - [ ] 修改 RouteTree 定义
  - [ ] 调整所有相关代码
  - [ ] 保持 API 向后兼容

- [ ] **性能测试**（1 天）
  - [ ] benchmark 验证收益
  - [ ] 监控编译时间和二进制大小

### 验收标准
- [ ] 动态分发减少 ≥50%
- [ ] 吞吐量提升 5-10%
- [ ] 编译时间增加 <30%
- [ ] 二进制大小增加 <20%

---

## P2: 编译器优化与性能监控（第 4 周）

**优先级**: P2 - 中优先级
**时间**: 2025-12-22 ~ 2025-12-31
**预期收益**: 3-5% 综合提升

### 子任务

#### 编译器优化
- [ ] **LTO 和优化配置**（1 天）
  - [ ] 更新 Cargo.toml profile.release
  - [ ] 配置 codegen-units=1, lto="fat"
  - [ ] 测试编译时间和性能收益

- [ ] **PGO（Profile-Guided Optimization）**（2 天）
  - [ ] 构建 PGO instrumented 版本
  - [ ] 运行 benchmark 收集 profile 数据
  - [ ] 使用 profile 数据重新编译
  - [ ] 验证性能提升

#### 性能监控体系
- [ ] **CI 集成 criterion benchmark**（1 天）
  - [ ] 在 CI 中运行 benchmark
  - [ ] 生成性能报告

- [ ] **性能回归检测**（1 天）
  - [ ] 设置性能阈值（-5% 触发警告）
  - [ ] 集成到 PR 检查流程

- [ ] **火焰图自动化**（1 天）
  - [ ] 添加 cargo-flamegraph 到工具链
  - [ ] 定期生成火焰图对比

### 验收标准
- [ ] LTO 配置生效，性能提升 2-3%
- [ ] PGO 编译成功，额外提升 1-2%
- [ ] CI benchmark 运行正常
- [ ] 性能回归检测工作

---

## 里程碑验收（v2.12）

### 定量指标
- [ ] **Box::pin 数量**: 从 91 处减少到 ≤10 处 ✅ **关键指标**
- [ ] **延迟 P50**: 降低 ≥15%
- [ ] **延迟 P99**: 降低 ≥15%
- [ ] **吞吐量**: 提升 ≥15%
- [ ] **堆分配次数**: 减少 ≥50%（通过火焰图）
- [ ] **CPU 使用率**: 降低 5-8%

### 定性指标
- [ ] **代码质量**:
  - [ ] `cargo clippy` 零 warning
  - [ ] 所有测试通过
  - [ ] 代码覆盖率不降低

- [ ] **文档完善**:
  - [ ] API 变更文档
  - [ ] 性能优化报告
  - [ ] 迁移指南（如果有 breaking changes）

### 基准测试场景
- [ ] **TechEmpower A**: GET / 返回 12B 文本
- [ ] **TechEmpower B**: 路径参数 + 查询参数 + JSON
- [ ] **深层路由**: 10 层嵌套路由匹配
- [ ] **高并发**: 1000+ 并发连接

---

## 风险与依赖

### 高风险项
1. **RPITIT 兼容性**
   - 依赖: Rust 1.75+
   - 影响: 如果用户使用旧版本 Rust，可能无法编译
   - 缓解: MSRV 策略明确，提供迁移指南

2. **大规模 API 变更**
   - 影响: 可能破坏下游用户代码
   - 缓解: 语义化版本控制，充分测试

### 依赖项
- Rust 工具链 ≥ 1.75
- criterion (benchmark)
- cargo-flamegraph (性能分析)
- wrk/wrk2 (HTTP 压测)

---

## 参考资料
- [PLAN.md](./PLAN.md) - 完整的项目规划
- [async_trait 性能问题](https://github.com/dtolnay/async-trait/issues/1)
- [RPITIT stabilization](https://blog.rust-lang.org/2023/12/21/async-fn-rpit-in-traits.html)
- [Rust Performance Book](https://nnethercote.github.io/perf-book/)
- benchmark 结果: `benchmark/target/criterion/`
